services:
  accountinfo:
    build: .
    image: ${CONTAINER_IMAGE:-karllee830/sub2api-accountinfo:latest}
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    ports:
      - "${HOST_PORT:-8080}:8080"
